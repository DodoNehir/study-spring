spring.application.name: crypto-dashboard

price-monitor:
  api-uri: http://${PRICE-MONITOR-HOST:localhost}:${PRICE-MONITOR-PORT:8888}/api/v1/prices/{cryptoUnit}-USD/market-price

currency-monitor:
  api-uri: http://${CURRENCY-MONITOR-HOST:localhost}:${CURRENCY-MONITOR-PORT:9999}/api/v1/currencies/{currencyUnit}